name: collector_flutter_example
description: Example app demonstrating collector_flutter usage.
version: 1.0.0+1
publish_to: none

environment:
  sdk: ">=3.5.0 <4.0.0"
  flutter: ">=3.24.0"

dependencies:
  flutter:
    sdk: flutter
  collector_flutter:
    path: ../
  http: ^1.5.0

dev_dependencies:
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
